return {
  "ibhagwan/fzf-lua",
  opts = {
    -- Global defaults for all pickers (files, grep, lsp, etc.)
    defaults = {
      formatter = "path.filename_first",
      -- path_shorten = 5
    },
  },
}
